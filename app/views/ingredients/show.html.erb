<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="turbo-visit-control" content="reload">
  </head>
<body>

<% content_for(:title, t('.title')) %>
<div class="top-header"></div>
<div class="container">
<hgroup>
  <h2><%= t '.title' %></h2><br>
</hgroup>
  <div class = 'ingredients_list'>
  <h3><%= @ingredient.name %> が含まれる市販医薬品一覧</h3>

    <% @ingredient.drugs.each do |drug| %>
    <dl>
      <dt>
      <%= link_to drug.name, drug_path(drug) %>
      </dt>
      <dd>
        あああ
      </dd>
    </dl>
    <% end %>
  </div>
  </div>
</body>