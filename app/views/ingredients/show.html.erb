<!DOCTYPE html>
<html>
  <head>
    <meta charset="UTF-8" />
    <meta name="turbo-visit-control" content="reload">
  </head>
<body>

<% content_for(:title, @ingredient.name + 'に含まれる医薬品一覧' ) %>
<div class="top-header"></div>
<div class="container">
<hgroup>
    <div class='midashi'><%= image_tag '眼鏡サーチ1.png', class: 'midashi_img'  %><%= 'おくすりの成分についての基礎知識' %></div><br>
</hgroup>
  <div class = 'ingredients_list'>
    <%= @ingredient.name %>
  <%= @ingredient.name %> が含まれる市販医薬品一覧

    <% @ingredient.drugs.each do |drug| %>
    <dl>
      <dt>
      <%= link_to drug.name, drug_path(drug) %>
      </dt>
      <dd>
      </dd>
    </dl>
    <% end %>
  </div>
  </div>
</body>