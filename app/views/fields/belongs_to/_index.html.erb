<%#
＃Belongsto Index Partial

この部分的なレンダリングは、belonds_to関係をレンダリングします。
リソースのインデックスページに表示されます。

デフォルトでは、関係は関連するオブジェクトへのリンクとしてレンダリングされます。

##ローカル変数：

 - 「フィールド」：
   [administrate :: field :: belongsto] [1]のインスタンス。
   データベースから引っ張られたbelonds_to関係の周りのラッパー。
%>

<% if field.data %>
  <% if accessible_action?(field.data, :show) %>
    <%= link_to(
      field.display_associated_resource,
      [namespace, field.data],
    ) %>
  <% else %>
    <%= field.display_associated_resource %>
  <% end %>
<% end %>
