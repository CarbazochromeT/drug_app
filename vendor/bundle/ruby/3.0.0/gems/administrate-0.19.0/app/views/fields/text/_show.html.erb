<%#
＃テキストは部分的に表示されます

この部分的なものは、テキスト属性をレンダリングします。
リソースのショーページに表示されます。

デフォルトでは、属性は、Whitespaceが保存されたテキストとしてレンダリングされます。
## Local variables:

- `field`:
  An instance of [Administrate::Field::Text][1].
  A wrapper around the Text pulled from the database.

[1]: http://www.rubydoc.info/gems/administrate/Administrate/Field/Text
%>

<div class="preserve-whitespace"><%- field.data.html_safe %></div>
